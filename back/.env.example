# Django
SECRET_KEY=cambia-esta-clave-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=kyocera_mantenimiento
DB_USER=postgres
DB_PASSWORD=Enano1314
DB_HOST=localhost
DB_PORT=5432

# CORS (origen del frontend)
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Celery / Redis (notificaciones asíncronas)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# Correo (en desarrollo se imprime en consola; en producción configurar SMTP)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=mantenimiento@tuempresa.com

# Integración futura con ECM (Alfresco)
DOCUMENT_STORAGE_BACKEND=local
ALFRESCO_BASE_URL=
ALFRESCO_USER=
ALFRESCO_PASSWORD=
